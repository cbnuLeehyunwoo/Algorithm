#include <iostream>
#include <algorithm>
#include <vector>
#include <iomanip>

using namespace std;

int main() {
	 float a = 0;
	 float b = 0;
	 cin >> a >> b;
	 cout << fixed << setprecision(1) << a * b * 1 / 2;
}