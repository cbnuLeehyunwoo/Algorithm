#include <iostream>
#include <string>
#include <vector>
#include <algorithm>
#include <cmath>
using namespace std;

int main() {
    long int A = 0;
    long int B = 0;
    cin >> A >> B;

    cout << (A + B) * (A - B);

 	return 0;
}