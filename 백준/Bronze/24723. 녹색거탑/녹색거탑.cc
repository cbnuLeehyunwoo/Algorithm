#include <iostream>
#include <cmath>

using namespace std;

int main() {
	int N = 0;
	cin >> N;
	//int count = 0;
	//for (int i = 1; i <= N; i++) {
	//	count = (i * 2);
	//}
	cout << pow(2, N);
}