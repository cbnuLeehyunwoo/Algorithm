#include <iostream>
#include <string>
using namespace std;

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);
	string str;
	cin >> str;
	cin >> str;
	cin >> str;
	for (int i = 0; i < 10000; i++) {
		cout << -1 << "\n";
	}
}