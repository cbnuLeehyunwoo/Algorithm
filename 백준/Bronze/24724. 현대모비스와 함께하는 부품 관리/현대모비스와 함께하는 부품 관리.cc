#include <iostream>
#include <string>

using namespace std;

int main() {
	int N = 0;
	cin >> N;
	for (int i = 0; i < N; i++) {
		cout << "Material Management " << i + 1 << "\n" << "Classification ---- End!\n";
	}
}