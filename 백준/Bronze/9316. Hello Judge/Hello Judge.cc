#include <iostream>
#include <string>
#include <math.h>
using namespace std;

int main() {
	int N = 0;
	cin >> N;
	for (int i = 1; i <= N; i++) {
		cout << "Hello " << "World, " << "Judge " << i << "!\n";
	}
}