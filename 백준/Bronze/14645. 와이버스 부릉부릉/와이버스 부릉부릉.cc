#include <iostream>

using namespace std;

int main() {
	int N = 0; // 정거장의 수
	int M = 0; // 거쳐가는 정거장
	int K = 0; // 탑승자의 수
	int psngr = 0; // 승객의 총 합
	
	cin >> N >> K;
	for (int i = 0; i < N; i++) {
		cin >> M >> K;
	}
	cout << "비와이";
}